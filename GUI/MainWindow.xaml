<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open" Click="OpenFileDialog"></MenuItem>
                <MenuItem Header="_Save" Click="SaveFileDialog"></MenuItem>
                <Separator></Separator>
                <MenuItem Header="_Exit"></MenuItem>
            </MenuItem>
        </Menu>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            
            <!-- Row0 begin -->
            <Button 
                Background="Beige" Foreground="Blue" FontWeight="Bold"
                Margin="10" FontSize="20" Click="Compile2PCode"
            >
                Compile
            </Button>
            <Button
                Grid.Row="0" Grid.Column="1"
                Background="Beige" Foreground="Blue" FontWeight="Bold"
                Margin="10" FontSize="20" Click="RunAll"
            >
                Run All
            </Button>
            <Button
                Grid.Row="0" Grid.Column="2"
                Background="Bisque" Foreground="Teal" FontWeight="Bold"
                Margin="10" FontSize="20" Click="Ttest"
            >
                Run By Step
            </Button>
            <!-- Row0 end -->
            
            <!-- Row1 begin -->
            <TextBlock Grid.Row="1" Grid.Column="0" Margin="10,0">
                Source Code
            </TextBlock>
            <TextBlock Grid.Row="1" Grid.Column="1" Margin="10,0">
                P-Code
            </TextBlock>
            <TextBlock Grid.Row="1" Grid.Column="2" Margin="10,0">
                Error
            </TextBlock>
            <!-- Row1 end -->
            
            <!-- Row2 begin -->
            <TextBox Grid.Row="2" Grid.Column="0" Margin="10" Name="Source"></TextBox>
            <TextBox Grid.Row="2" Grid.Column="1" Margin="10" Name="PCode"></TextBox>
            <TextBox Grid.Row="2" Grid.Column="2" Margin="10" Name="Error" IsEnabled="False"></TextBox>
            <!-- Row2 end -->
            
            <!-- Row3 begin -->
            <TextBlock Grid.Row="3" Grid.Column="0" Margin="10">
                Input
            </TextBlock>
            <TextBlock Grid.Row="3" Grid.Column="1" Margin="10">
                Heap
            </TextBlock>
            <TextBlock Grid.Row="3" Grid.Column="2" Margin="10">
                Output
            </TextBlock>
            <!-- Row3 end -->
            
            <!-- Row4 begin -->
            <TextBox Grid.Row="4" Grid.Column="0" Margin="10" Name="Input" TextChanged="Input_OnTextChanged" IsEnabled="False"></TextBox>
            <TextBox Grid.Row="4" Grid.Column="1" Margin="10" Name="Heap" IsEnabled="False"></TextBox>
            <TextBox Grid.Row="4" Grid.Column="2" Margin="10" Name="Exec" IsEnabled="False"></TextBox>
            <!-- Row4 end -->
            
        </Grid>
    </DockPanel>
</Window>
